# --- Core scientific stack ---
numpy>=1.26
pandas>=2.2
scipy>=1.13

# --- HMM ---
hmmlearn>=0.3.2

# --- Technical indicators (ADX, RSI, ROC for the HMM feature set) ---
ta>=0.11

# --- Config / models ---
pydantic>=2.6
python-dotenv>=1.0
PyYAML>=6.0

# --- Storage ---
pyarrow>=15.0

# --- Terminal dashboard + scheduling ---
rich>=13.7
schedule>=1.2
websocket-client>=1.7

# --- Broker (Trade212): reuse local package as an editable install ---
-e /home/jawahar/trading/trade212

# --- Dev / test ---
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
